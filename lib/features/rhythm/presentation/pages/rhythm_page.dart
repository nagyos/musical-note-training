import 'package:flutter/material.dart';

class RhythmPage extends StatelessWidget {
  const RhythmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Rhythm — v1.2')),
    );
  }
}