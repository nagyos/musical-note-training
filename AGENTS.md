# エージェント向けルール（musical-note-training）

このファイルは Grok が**セッション開始時に自動読み込み**するプロジェクト指示である。
（`grok inspect` で確認可能。`CLAUDE.md` は互換用の別名だが、本リポジトリでは `AGENTS.md` を正とする。）

ユーザーとのやり取り・意思決定・ドキュメント更新は、ここに従う。

---

## 1. 基本方針

### 事実ベースで進める

- **推測・あてずっぽうで実装や方針決定をしない。**
- 根拠がない場合は、コード・ドキュメント・実行結果を確認してから述べる。
- 確認できない事項は「未確認」「未決定」と明示し、ユーザーに質問する。
- 仮定で進める必要がある場合は、**仮定であることを明示**し、ユーザーの確認を取ってから実装する。

### 手戻りを防ぐ・振り返り可能にする

- 後から「いつ・誰が・何を決めたか」追えるよう、重要な Q&A と決定は `docs/decisions.md` に残す。
- 実装前に手戻りリスクが高い論点（DB スキーマ、UX フロー、課金、同期、破壊的変更）は**必ずユーザーに確認**する。
- 決定に反する実装をしない。矛盾を見つけたら作業を止め、ユーザーに確認する。

### ドキュメントの役割分担

| ファイル | 用途 |
|----------|------|
| `AGENTS.md`（本ファイル） | エージェントの振る舞い・テンプレート・確認ルール（自動読み込み） |
| `docs/decisions.md` | 技術選定・未決定事項・**ユーザーとの Q&A ログ** |
| `docs/tasks.md` | タスク状態・着手順の更新 |
| `docs/functional-requirements.md` | 機能要件の追記（スコープ変更時） |
| `docs/architecture.md` | 構成・依存ルールの追記（構造変更時） |

**新規ドキュメントは原則作らない。** 既存ファイルへの追記・差分更新で足りる情報だけ残す。

---

## 2. ユーザーへの確認が必要なタイミング

以下に該当する場合、**実装前に必ず質問**する。質問なしで進めない。

| カテゴリ | 例 |
|----------|-----|
| スコープ | MVP に含める / 含めない、フェーズの繰り上げ・延期 |
| UX・仕様 | 画面フロー、デフォルト値、エラー時の挙動、文言 |
| データ設計 | モデル構造、ID 形式、削除方針、マイグレーション |
| 技術選定 | ライブラリ追加、Turso/同期方式、描画・音声の実装手段 |
| 破壊的変更 | 既存 API・スキーマ・ルーティングの変更 |
| 外部依存 | ストア申請、広告 SDK、認証、課金、プライバシー |
| ドキュメント矛盾 | `decisions.md` と `tasks.md` / 実装が食い違う |
| 複数の妥当な選択肢 | トレードオフがあり、ユーザーの意向が必要 |

### 確認不要で進めてよい場合

- `docs/` に**既に明文化された決定**があり、矛盾がない
- ユーザーが**直前のメッセージで明示的に指示**した内容
- 機械的な作業（lint 修正、指示どおりのリネーム、テスト追加のみ等）
- PoC の範囲が Issue / タスクに書かれており、逸脱しない

---

## 3. 質問のテンプレート

ユーザーへの確認は、次の形式で行う。

```markdown
## 確認: [タスク ID または論点]

**背景**
（なぜ今決める必要があるか。1〜2 文）

**現状の事実**
- （コード・ドキュメント・実行結果から分かっていること）
- （未確認・未決定のことはその旨を明記）

**選択肢**
| 案 | メリット | デメリット |
|----|----------|------------|
| A: … | … | … |
| B: … | … | … |

**推奨**（根拠付き。推奨できない場合は「判断保留」と書く）

**質問**
1. …
2. …（必要なら）
```

- 選択肢は 2〜3 個に絞る。
- 推奨は事実とトレードオフに基づく。根拠が薄い推奨はしない。
- 1 メッセージに論点を詰め込みすぎない。独立した論点は分けて質問する。

---

## 4. 回答の記録テンプレート（`docs/decisions.md`）

ユーザーが回答したら、`docs/decisions.md` の **「Q&A ログ」** セクションに追記する。

```markdown
### [YYYY-MM-DD] [タスク ID] 論点の短いタイトル

**質問**
- …

**回答（ユーザー）**
- …

**決定**
- …

**根拠・補足**
- …

**影響**
- 更新: `docs/tasks.md` T-xxx / `docs/architecture.md` §… / コード `lib/...` など
```

- **決定**はユーザーの回答を要約した事実。エージェントの解釈は **根拠・補足** に分ける。
- タスク状態が変わったら `docs/tasks.md` も更新する。
- 機能スコープが変わったら `docs/functional-requirements.md` に最小限追記する。

---

## 5. 作業フロー（タスク着手時）

```
1. タスクのスコープと受け入れ条件を確認（tasks.md / Issue）
2. 関連する decisions.md・architecture.md を読む
3. 「確認が必要なタイミング」に該当するか判定
   → 該当: 質問テンプレートでユーザーに確認 → 回答を decisions.md に記録
   → 非該当: 既存の決定に従い実装
4. 実装
5. 完了時: tasks.md の状態更新、必要なら decisions.md に実装メモを追記
```

---

## 6. 回答作成時の規則（エージェント自身への指示）

### 構成

1. **結論**（1〜2 文）
2. **事実**（確認済みの内容。推測は含めない）
3. **提案・次のアクション**（未決定部分は質問として明示）

### 禁止・注意

- 未読のファイルの内容を断定しない。
- ユーザーの意向を勝手に決めない（「おそらく A でしょう」で A を実装しない）。
- ドキュメントにない機能を「ある」と言わない。コードにない実装を「済み」と言わない。
- 大きな変更は差分の意図を説明し、どの決定（decisions.md のエントリ）に基づくか示す。

### コード・ドキュメント変更時

- 変更理由を 1 文で書く。
- 関連タスク ID（`T-xxx`）をコミットメッセージまたは PR に含める（`contributing.md` 参照）。
- `flutter pub add` 等でパッケージを追加したら、`docs/contributing.md` の「パッケージ導入ログ」に**実行コマンドを追記**する。

---

## 7. コード品質・設計原則（実装時は必ず順守）

### 目標

**クリーンで疎結合な実装**を維持し、保守・スケール（機能追加）がしやすいコードベースにする。  
「動けばよい」実装や、将来の負債になる手抜きは避ける。

### 必須原則

| 原則 | 具体ルール |
|------|------------|
| **依存の方向** | `features → shared → core` のみ。逆方向 import 禁止（`architecture.md`） |
| **単一責任** | 1 ファイル / 1 クラスは 1 つの理由で変わるようにする |
| **疎結合** | 具象（DB・JSON・Widget）に依存せず、抽象（Repository 等）経由でアクセス |
| **ドメインロジックの分離** | 正誤判定・セッション進行・スコア計算は `domain/` の純 Dart に置く。Widget に書かない |
| **定数管理** | マジックナンバーは `architecture.md` の置き場に従い名前付き定数へ |
| **UI は薄く** | `presentation/` は表示とユーザー操作のみ。データ取得・変換は ViewModel / Repository へ |
| **テスト可能な形** | 副作用のある処理は差し替え可能（Repository 抽象、Provider 注入）にする |
| **過剰設計をしない** | 使われない抽象・汎用化・ premature optimization は入れない（下記「見直し」参照） |
| **DDD（必要時）** | 複雑なユースケースは `features/*/domain/` に集約し TDD で先に固める。全 feature に UseCase 必須化はしない |

### 実装前チェック（エージェント自身）

```
- 既存の命名・パターン・レイヤー配置に合っているか？
- ビジネスロジックが Widget / Painter に漏れていないか？
- 新しい依存（パッケージ・層間 import）は必要最小限か？
- 定数は適切な塊（AppSpacing / StaffMetrics 等）に置いたか？
- ドメインロジックにユニットテストを足すべきか？（§8 参照）
```

### ルールの見直し

このルールが**過剰・不適切**と判断した場合は、黙って従わずユーザーに指摘する。

- 例: 1 回しか使わない 10 行の UI に Repository を挟むのは過剰
- 例: PoC 段階の楽譜チューニングにフル TDD はコスト > 利益
- 指摘時は代替案とトレードオフを示し、`decisions.md` に記録する

詳細な構成ルールは [architecture.md](./docs/architecture.md) を参照。

---

## 8. テスト方針（TDD）

**テスト駆動が強く推奨される領域では、Red → Green → Refactor で進める。**  
それ以外はハイブリッド（実装後テスト）でよい。

| 領域 | 方針 | 理由 |
|------|------|------|
| `features/*/domain/`（クイズ・デッキ・セッション等） | **TDD 必須**（テストを先に書く） | ロジックが複雑化しやすく、回帰バグの影響が大きい |
| `shared/domain/` の振る舞い・バリデーション | **TDD 必須** | 仕様がコードで表現される |
| `shared/data/repositories/` | **テストファースト推奨**（in-memory DB 等） | データの正しさが UX に直結 |
| `shared/domain/models/`（fromJson 等） | 実装と同時 or 直後にテスト | スキーマが固定されやすい |
| `presentation/`（Widget） | クリティカルパスのみ Widget テスト | 全画面 TDD はコスト高 |
| 楽譜描画・UI 調整 | 手動確認 + レイアウトテスト最小限 | 見た目の試行錯誤が多い |

### 実務フロー

```
1. domain / repository（推奨領域）: 失敗するテストを書く → 最小実装 → リファクタ
2. presentation: 実装 → 主要フローのテスト追加
3. 完了前: flutter test + dart analyze 必須
```

参照実装: `StudySessionLogic` + `study_session_logic_test.dart`

### DDD との併用

- ロジックが feature をまたぐ・複数 Repository を組み合わせる場合は **`domain/` にアプリケーションサービス**（例: `StudyLaunchService`）を置き、**そこを TDD で先に固める**。
- 厳密な DDD 全体設計より、**必要な feature の domain を厚くする**ことを優先（`architecture.md` 参照）。

---

## 9. フェーズ別の確認の深さ

| フェーズ | 事前確認の深さ |
|----------|----------------|
| Phase 0 | DB・データモデル・楽譜描画など手戻りコストが大きい論点は必ず確認 |
| Phase 1 | 画面フロー・データ形式・受け入れ条件をタスク単位で確認 |
| Phase 2 | 新モードの UX と既存フローとの関係を確認 |
| Phase 3 | IAP・認証・クラウド・法務関連は必ず確認 |
| Phase 4 | コンテンツ拡張のスコープのみ軽く確認 |

詳細な進め方は `docs/decisions.md` の「フェーズ別作業整理」を参照。

---

## 10. Git / Issue 運用（エージェント向け）

### ブランチとマージ

```text
develop から issue/N を切る → 実装・push（feature ブランチ上で反復）
  → 動作確認（flutter run 等）で問題なさそうなら develop にマージ
develop → main はユーザー承認付き PR のみ（エージェントは勝手にマージしない）
```

| 操作 | ルール |
|------|--------|
| `issue/N` 上での作業 | エージェントのデフォルト。**`develop` へはマージしない** |
| `issue/N` → `develop` | **ユーザーが問題なしと判断した後**（または明示指示後）。`dart analyze` + `flutter test` 通過が前提 |
| `develop` → `main` | **ユーザー確認後** PR マージ |
| 着手前 | `develop` を pull。`grok inspect` で `AGENTS.md` を確認 |
| マージ前 | `flutter test` + `dart analyze` 必須。UI・微調整は feature ブランチで `flutter run` 確認 |

**エージェント禁止**: テストが通っただけで自動的に `develop` へマージしない。微調整・見た目確認が未完の変更を `develop` に入れない。

### 順序

- 1 Issue ＝ 1 `issue/N` ブランチ。ブランチ上でコミット・push し、**確認できてから** `develop` に統合する。
- 依存がある Issue は、先の `issue/N` が `develop` に入ってから次を切る（例: Phase 0 → 学習 → デッキ）。
- 長期間の未コミット変更や `stash` の持ち越しは避ける（コンフリクトの主因になりやすい）。

### コミット

- 形式: `feat(scope): 概要 (issue/N) #N`（[contributing.md](./docs/contributing.md) 参照）
- 1 コミット = 1 Issue 単位でもよい（個人開発・縦スライス単位）

---

## 11. 関連ドキュメント

- [docs/README.md](./docs/README.md) — ドキュメント索引
- [docs/decisions.md](./docs/decisions.md) — 決定事項・Q&A ログ
- [docs/tasks.md](./docs/tasks.md) — タスク一覧
- [docs/contributing.md](./docs/contributing.md) — ブランチ・コミット規約・パッケージ導入ログ